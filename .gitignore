node_modules/
.next/
dist/
.turbo/
.env
.env.local
.env*.local
.vercel
.DS_Store
*.tsbuildinfo

# Convex's agent skills, vendored by the `skills` CLI. Third-party content with
# its own license — tracked as a lockfile, not republished from this repo.
# Restore with: npx skills add get-convex/agent-skills
apps/web/.claude/
apps/web/.agents/
