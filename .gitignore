node_modules/
.next/
dist/
.turbo/
.env
.env.local
.env*.local
.vercel
.DS_Store
*.tsbuildinfo

# Convex's agent skills, vendored from get-convex/agent-skills. Third-party
# content under its own license, so this repo tracks the lockfile
# (apps/web/skills-lock.json) rather than republishing the files.
# Restore with: cd apps/web && npx convex ai-files install
apps/web/.claude/
apps/web/.agents/
